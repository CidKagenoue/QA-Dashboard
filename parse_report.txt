Parse report
============

Source files: gpp.txt, jap.txt
Preview generated files: gpp_parsed_preview.json, jap_parsed_preview.json

Observations:
- Rows inspected: first 10 data rows from each file.
- All inspected rows contain explicit "Startdatum" and "Einddatum" values in the source files (format d.m.yyyy).
- All inspected Startdatum/Einddatum were converted to ISO (YYYY-MM-DD).
- "Jaar" column sometimes contains a range (e.g., "2026-2030"); previews mapped to startYear/endYear accordingly.

Implication:
- The original TXT files have full dates; if your app ends up storing only the year, the bug is in the import/parsing code (it drops or truncates day/month). The source data is fine.

Next steps you can ask me to run (pick one):
- Generate full parsed JSON for all rows (not only preview).
- Create a one-off import script that maps and writes GPP entries to the database preserving full dates.
- Show the exact lines in your backend import code that parse dates so I can patch them.

Tell me which of the three you want me to do now.